/** No single delivery, spoilage or count at a butcher is this big; past it, it's a typo (4500 typed twice). */
export const MAX_STOCK_CHANGE_G = 500_000;
export const MAX_STOCK_CHANGE_UNITS = 2_000;
